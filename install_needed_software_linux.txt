#This files explains how to install the Linux software that will enable you to work with the ESP8266 'robot brain'

On Ubuntu:

- Install pillow: `sudo pip install pillow`
- Optional: install pinta: `sudo apt install pinta`
