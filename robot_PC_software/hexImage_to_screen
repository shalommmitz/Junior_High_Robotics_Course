fn = "image.hex"
f = open(fn)
pic = f.read().split()
f.close()

for y in range(48):
    line = pic[y].strip()
    pixels = bin( int(line,16) )[2:].zfill(64)
    for x in pixels:
        print x,
    print
